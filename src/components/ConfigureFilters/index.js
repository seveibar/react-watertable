// @flow

import React from "react"
import ConfigureButtonBase from "../ConfigureButtonBase"
import FilterIcon from "@material-ui/icons/FilterList"

export const ConfigureFilters = () => {
  return null
}

export const ConfigureFiltersButton = () => {
  return <ConfigureButtonBase Icon={FilterIcon} text="Filters" />
}
